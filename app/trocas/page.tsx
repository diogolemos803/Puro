import { PlaceholderPage } from "@/components/PlaceholderPage";

export const metadata = { title: "Trocas e devoluções — Puro Society" };

export default function TrocasPage() {
  return (
    <PlaceholderPage title="Trocas e devoluções">
      <p>
        Cada peça é feita sob encomenda para o pedido — condições de troca são avaliadas caso a
        caso, direto pelo contato.
      </p>
    </PlaceholderPage>
  );
}
